# In dev, use a relative path so requests go through the Vite proxy (see vite.config.ts → server.proxy)
# In prod, set this to the absolute API URL (e.g. https://api.example.com)
VITE_API_URL="/api"

VITE_DATA_API_URL="/data/api"

API_PROXY_TARGET="http://app:3000"

VITE_MATCH_THRESHOLD=40